<div class="mb-8">
    <h1 class="text-4xl md:text-5xl font-bold mb-2">
        {{ $title }}
    </h1>
    @if(isset($subtitle))
        <p class="text-gray-300 text-lg">{{ $subtitle }}</p>
    @endif
</div>
